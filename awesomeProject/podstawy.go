package main

import (
	"fmt"
	"math"
)

func funkcja(x int, y int, z int) (int, int) {
	var a int
	fmt.Println("Hello World")
	t := math.Sqrt(x)
	u := int(t)
	i := 1
	for i, v := range {1,2} {
		fmt.Println(i)
		fmt.Println(v)
	}
	if i < 10 {

	} else {

	}
	b := "0"
	switch b {
	case "0":
		fmt.Println("0")
	case "1":
		fmt.Println("1")
	default:
		fmt.Println("żaden")
	}

	var wsk *int

	p := *wsk

	type Kwadrat struct {
		x int
		y int
	}

	var lista [10]int

	lista[0] = 0
	var mapa map[string]int
	mapa["Hello"] = 0

	defer fmt.Println("World")
	fmt.Println("Hello ")

	return x + y, y + z
}
